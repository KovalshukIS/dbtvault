name: automate_dv
version: 0.9.7
require-dbt-version: [">=1.0.0", "<2.0.0"]
config-version: 2

source-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
data-paths: ["seeds"]
macro-paths: ["macros"]
docs-paths: ["docs"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
